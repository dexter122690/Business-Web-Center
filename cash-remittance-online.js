/* Shows cash received from invoice payments until an owner/admin records remittance. */
(function(){
  var config=window.BUSINESS_WEB_CENTER_SUPABASE||{},db=null,businessId='',userId='',rows=[];
  function peso(value){return 'PHP '+Number(value||0).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}
  function esc(value){return String(value==null?'':value).replace(/[&<>"']/g,function(ch){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]})}
  function activeBranch(){return localStorage.getItem('bwc-active-branch')||''}
  function period(row){var month=(document.getElementById('dm')||{}).value||'',year=(document.getElementById('dy')||{}).value||'',date=new Date(row.received_date+'T00:00:00');return (!month||date.getMonth()===Number(month))&&(!year||String(date.getFullYear())===String(year))}
  async function load(){if(!db||!businessId||!activeBranch())return;var result=await db.from('cash_collections').select('id,received_date,amount,received_by,status,remitted_at,invoice_id,invoices!inner(invoice_number,client_name)').eq('business_id',businessId).eq('branch_id',activeBranch()).order('received_date',{ascending:false});if(result.error)return;rows=result.data||[];draw()}
  function draw(){var dashboard=document.getElementById('dashboard');if(!dashboard)return;var current=rows.filter(period),pending=current.filter(function(row){return row.status==='Pending remittance'}),remitted=current.filter(function(row){return row.status==='Remitted'}),pendingTotal=pending.reduce(function(sum,row){return sum+Number(row.amount||0)},0),remittedTotal=remitted.reduce(function(sum,row){return sum+Number(row.amount||0)},0),box=document.getElementById('cashRemittanceCard');if(!box){box=document.createElement('div');box.id='cashRemittanceCard';box.className='card';box.style.marginTop='14px';var after=dashboard.querySelector('.split');if(after&&after.parentNode)after.parentNode.insertBefore(box,after.nextSibling);else dashboard.appendChild(box)}box.innerHTML='<div class="k">Cash remittance</div><h2>Cash received from clients</h2><div class="grid"><div class="mini"><div class="muted">Waiting for owner remittance</div><div class="metric">'+peso(pendingTotal)+'</div><small>'+pending.length+' cash payment'+(pending.length===1?'':'s')+'</small></div><div class="mini"><div class="muted">Already remitted</div><div class="metric">'+peso(remittedTotal)+'</div><small>'+remitted.length+' payment'+(remitted.length===1?'':'s')+'</small></div></div>'+(current.length?'<div style="overflow:auto;margin-top:14px"><table><thead><tr><th>Received</th><th>Invoice / client</th><th>Received by</th><th>Cash amount</th><th>Status</th><th>Action</th></tr></thead><tbody>'+current.map(function(row){var invoice=row.invoices||{},pending=row.status==='Pending remittance';return '<tr><td>'+esc(row.received_date)+'</td><td><b>INV-'+String(invoice.invoice_number||'').padStart(5,'0')+'</b><br><small>'+esc(invoice.client_name||'Client')+'</small></td><td>'+esc(row.received_by||'—')+'</td><td><b>'+peso(row.amount)+'</b></td><td><span class="badge">'+esc(row.status)+'</span></td><td>'+ (pending?'<button class="primary" data-cash-remit="'+row.id+'">Mark remitted</button>':'<small>'+esc(row.remitted_at?new Date(row.remitted_at).toLocaleString():'Recorded')+'</small>')+'</td></tr>'}).join('')+'</tbody></table></div>':'<div class="empty" style="margin-top:14px">No cash payments were received in this selected period.</div>')}
  async function remit(id){var row=rows.find(function(item){return item.id===id});if(!row||!confirm('Mark '+peso(row.amount)+' as remitted to the business owner?'))return;var result=await db.from('cash_collections').update({status:'Remitted',remitted_at:new Date().toISOString(),remitted_by:userId}).eq('id',id).eq('business_id',businessId);if(result.error){alert('The remittance could not be updated. '+result.error.message);return}await load();alert('Cash remittance recorded. The dashboard has been updated.')}
  async function start(){if(!window.supabase||!config.url||!config.publishableKey){setTimeout(start,300);return}db=window.businessSupabase||window.supabase.createClient(config.url,config.publishableKey);var session=await db.auth.getSession(),user=session.data&&session.data.session&&session.data.session.user;if(!user)return;userId=user.id;businessId=localStorage.getItem('bwc-active-business')||'';await load();document.addEventListener('bwc:invoices-loaded',function(){setTimeout(load,100)});document.addEventListener('click',function(event){var button=event.target.closest('[data-cash-remit]');if(button){event.preventDefault();remit(button.dataset.cashRemit);return}if(event.target.closest('[data-t="dashboard"]'))setTimeout(draw,100)});document.addEventListener('change',function(event){if(['dm','dy'].includes(event.target.id))draw()})}
  setTimeout(start,900);
})();
